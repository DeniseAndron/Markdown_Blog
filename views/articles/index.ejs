<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">

    <title>Blog</title>
</head>
<body>
    <div class="container">
        <!-- This is the header of the blog-->
        <h1 class="mb-4">Blog Articles</h1>
        <!-- This is the button New Article with the path set up and the style from bootstrap-->
        <a href ="/articles/new" class="btn btn-success">New Article</a>

        <!-- looping through articles and creating-->
        <% articles.forEach(article => { %>
            <div class="card mt-4">
                <div class ="card-body">
                    <!-- here we will have our text, body-->
                    <h4 class="card-title"><%= article.title %></h4>
                    <div class="card-subtitle text-muted mb-2">
                        <!-- tolocaledatestring converts the date to the local one 3/5/2020-->
                        <%= article.createdAt.toLocaleDateString() %>
                </div>
                <!-- The description of the blog, routing from the server.js-->
                <!-- The button Read more-->
                <div class="card-text mb-2"><%= article.description%></div>
                    <a href="articles/<%= article.slug %>"
                        class="btn btn-primary">Read More</a>
                        <a href="articles/edit/<%= article.id %>"
                            class="btn btn-info">Edit</a>
                        <!-- using a form to delete a route, because if using href, google will delete everything in the database-->
                        <form action = "/articles/<%= article.id %>?_method=DELETE" method="POST" class="d-inline">
                            <button type="submit" class="btn btn-danger">Delete</button>
                        </form>
                    </div>
        </div>
    <% })%>
</div>
</body>
</html>